<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_main"
    android:layout_height="match_parent"
    android:layout_width="match_parent"

    android:orientation="horizontal">


    <ScrollView
        android:id="@+id/ScrollView01"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fillViewport="true"
        android:scrollbars="none">

        <RelativeLayout
            android:id="@+id/matchView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@mipmap/background4"
            />

    </ScrollView>
    <Button
        android:id="@+id/btnNextNumber"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:text="100"
        android:textSize="18dp"
        android:layout_marginLeft="10dp"
        android:textColor="@color/white"
        android:background="@mipmap/next_number_background"
        android:layout_centerVertical="true"/>

    <TextView
        android:id="@+id/btnScore1"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="5"
        android:textSize="32dp"
        android:textAlignment="center"
        android:textColor="@color/red"
        android:layout_centerVertical="true"
        android:layout_above="@id/btnNextNumber"/>

    <TextView
        android:id="@+id/btnScore2"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:text="7"
        android:textSize="32dp"
        android:textAlignment="center"
        android:gravity="bottom"
        android:textColor="@color/blue"
        android:layout_centerVertical="true"
        android:layout_below="@id/btnNextNumber"/>
</RelativeLayout>
